describe('Clocking API Routes', () => {
    test('Clocking Status', () => {
        expect(1).toBe(1);
    })

    test('Clocking List', () => {
        expect(1).toBe(1);
    })

    test('Clocking In', () => {
        expect(1).toBe(1);
    })

    test('Clocking Out', () => {
        expect(1).toBe(1);
    })

    test('Mobile Clocking', () => {
        expect(1).toBe(1);
    })
})