describe('User API Routes', () => {
    test('Get All Users', () => {
        expect(1).toBe(1);
    })

    test('Delete User by ID', () => {
        expect(1).toBe(1);
    })

    test('Get User by ID', () => {
        expect(1).toBe(1);
    })

    test('Register a User', () => {
        expect(1).toBe(1);
    })

    test('Log a User In', () => {
        expect(1).toBe(1);
    })

    test('Modify a User', () => {
        expect(1).toBe(1);
    })
})