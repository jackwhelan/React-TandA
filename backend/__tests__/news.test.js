describe('News API Routes', () => {
    test('Get News', () => {
        expect(1).toBe(1);
    })

    test('Delete News Item', () => {
        expect(1).toBe(1);
    })

    test('Add News Item', () => {
        expect(1).toBe(1);
    })
})