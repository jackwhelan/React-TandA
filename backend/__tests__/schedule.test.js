describe('Schedule API Routes', () => {
    test('Get Events', () => {
        expect(1).toBe(1);
    })

    test('Cancel Event by ID', () => {
        expect(1).toBe(1);
    })

    test('Schedule Event', () => {
        expect(1).toBe(1);
    })
})